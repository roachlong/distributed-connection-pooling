the requirement for multi-region has changed a bit and we need to update our design and sample workload.  essentially the customer has two use cases they need to satisfy on the same database.

1) implement smart client partitioning, but without the manual approach described in our demos folder.  i think we have the approach using computed columns and a stored region which is already in the initial-schema.sql design.  this will support the loading of data where the files that need to be loaded already have a primary key assigned, and the client will also need to provide a locality value when the data is loaded.  this will prevent cockroach from performing a UUID check across multi-region, but the client needs to be aware of the locality for every account if they want to keep transaction data local to the region where the app is running.

2) leverage multi-region abstractions and allow the database to partion data based on the locality of the client as data is written.  we also want to leverage local CDC publishing to region specific kafka topics so that application consumers are only receiveing updates / notifications for data that is local to their region.

i still want to leverage event logs workload and schema for our setup, but we need to change the DDL to align with the use cases above.

account_info: we'll use this table to simulate batch uploads using the locality flag approach.  i don't want to seed data in the populate-sample-data.sql script, other than the static gobal tables, we should pre-load them.  but everything else should start with our batch process to load account data.  that's one app that should be locality aware and will be run periodically to load the table.

request_info and request_account_link: i want another app that should run periodically on a schedule and will leverage our pagination solution, see the example at https://github.com/roachlong/distributed-transactions-poc/tree/main/DistributedTransactions.AllocationsApi, especially the TradeRepository.cs repo and the models that implement pagination for the program search function and the https://github.com/roachlong/distributed-transactions-poc/tree/main/DistributedTransactions.TradeViewer client that uses pagination, but i want our app to use locality aware pagination to consume the data loaded by batch, process the records to create account maintenance requests, and write them to these tables using MR abstractions with regional by row.  at the same time write an event to the request_event_log, which should cdc to regional kafka topics.

request_event_log: another app listening to the request_event_log will pick-up these messages and process a step in the workflow, updating the status, etc. and firing another event to request_event_log.  if the workflow simulator receives an event in a terminal status then no other events for that request will be fired. 

request_status_head: we should use a trigger on the request_info table to upsert our request_status_head

trade_info: another app listening on the request_status_head should maintain a list of active accounts (successfully completed requests) and generate random trade activity for the primary account and linked accounts.  note that CDC should publish messages with {"RequiredAcks": "ONE"} for locality and consumers should auto-commit but manually store the offset, after confiming that a message processed successfully.  see the TradeCaptureConfig.cs and program at https://github.com/roachlong/distributed-transactions-poc/tree/main/DistributedTransactions.TradeCapture for an example.

so the only table using computed locality partitions is the intial table that our batch writes to, account_info.  everything else is based on data hitting a table in a specific region that is published to a kafka topic for consumption in that region, again i don't want to use the computed locality flag for this, instead those events should CDC by crdb_region to topics for regionally aligned application instances to consume and process, writing to a final output table using MR abstrations that will auto-home data to the same region.

i need a another client app that will read from the locality aligned account_info table and join that to the trade_info table to aggregate trade data by multiple dimensions (account_name, strategy, symbol, currency, maybe even the region where the account live) into views that a user can drill into and see the details.  i would expect this client to also use our pagination technique, submitting multiple parallel requests for all pages and aggregate the results inside the app, leveraging dotnet tasks and when all complete logic, which should also be in some of the examples i've shared.  unlike other apps, this app will be going across regions, so will need to request info based on locality fields and will get responses back from global execution, but each query should only be getting data that is locally distributed.

the trick is that we need to ensure our locality aware apps only read and write data in the same region where they live.  so the batch loading app determines locality, and  ecah instance of our other data processing apps only work in a specific locality so that writes to the request, event and trade tables stay in the same region as the account info.

to show this i would also like a query file that shows the locality of accounts in each region and also includes how many leasehlder records from all of the other tables related to those "local" accounts exist across all the regions.  we should see zero records outside of the account's home region and that will confirm if our strategy works.  if there are any other checks we can perform to prove our location strategy please advise.

i've moved the previous implementation under the folder old-implementation so we cam start fresh but follow the same spirit in regards to structure and guidance for setting up and running the program.  let's do this!